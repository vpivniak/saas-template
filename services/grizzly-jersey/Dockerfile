FROM openjdk:8u151-jre-alpine

ARG GJ_ARTIFACT
ARG GJ_HOSTNAME
ARG GJ_PORT

ENV GJ_ARTIFACT $GJ_ARTIFACT
ENV GJ_HOSTNAME $GJ_HOSTNAME
ENV GJ_PORT $GJ_PORT

COPY ./target/${GJ_ARTIFACT} /${GJ_ARTIFACT}
CMD exec java -jar /${GJ_ARTIFACT}
